name: key-capability-gate

on:
  push:
  pull_request:

jobs:
  key-capability-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/parity/check_key_capabilities.sh

name: parity-ledger

on:
  push:
  pull_request:

jobs:
  parity-ledger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/parity/check_ledger_schema.sh
      - run: scripts/parity/check_fk_integrity.sh

name: phase-exit

on:
  push:
  pull_request:

jobs:
  phase-exit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        phase: [0, 1, 2, 3]
    steps:
      - uses: actions/checkout@v4
      - run: scripts/parity/check_phase_exit.sh ${{ matrix.phase }}

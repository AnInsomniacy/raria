name: claim-inventory

on:
  push:
  pull_request:

jobs:
  claim-inventory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/parity/build_claim_inventory.sh

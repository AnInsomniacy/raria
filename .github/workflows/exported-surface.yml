name: exported-surface

on:
  push:
  pull_request:

jobs:
  exported-surface:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/parity/build_live_rpc_manifest.sh
      - run: scripts/parity/check_exported_surface.sh

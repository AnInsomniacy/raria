# Modernization Documents

The active modernization tracker has moved to:

`workstreams/core-modernization`

The previous files in this directory were retired because they created a large
single-runbook authority that was hard to resume safely after context
compaction. Useful evidence from those documents was consolidated into the new
tracker's overview, ledgers, source evidence file, progress log, and checkpoint
CSV files.

Do not add new modernization progress here. Update the root workstream tracker
instead.

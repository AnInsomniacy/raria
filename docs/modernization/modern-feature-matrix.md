# Modern Feature Matrix

This matrix is the active completion contract for the raria modernization goal. aria2 is used as a feature reference only. raria public APIs, configuration, storage, and CLI must be native raria designs.

Status values:

- Covered: implemented in raria with tests or clear verification anchors.
- Partial: implemented but incomplete for the modern target.
- Gap: not implemented or still shaped by legacy compatibility.
- Excluded: intentionally out of scope as legacy or non-target behavior.

## Public Surfaces

| Capability | Modern target | raria status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Native HTTP control API | Stable `/api/v1` JSON API with raria-native resources | Partial | Daemon native smoke covers health, task listing, task creation with requested segments, Metalink bytes creation, Metalink mirror failover, Metalink checksum failures, pause, resume, remove, event stream, source-failure events, session save, explicit session save, restore, native shutdown, native segment resume, global transfer mutation, waiting-task transfer mutation, range source replacement, queue reorder, live BT metadata, live BT peers, live BT trackers, live BT file selection, BT tracker/fastresume/DHT setup through native creation, and `raria.toml` bearer auth; native contract tests cover config, stats, shutdown, global transfer policy, events, restart, queue position, file reads and selection updates, source reads and range source replacement, peers, tracker reads and tracker mutation, task transfer policy, BT creation options, BT seeding policy, Metalink bytes/path creation, Metalink checksums, Metalink invalid payload errors, and auth; JSON-RPC still remains without `aria2.addMetalink` or `aria2.addTorrent` | Migrate remaining daemon protocol tests from JSON-RPC to native API |
| Native event stream | WebSocket event schema with raria event names and typed payloads | Partial | `/api/v1/events` streams `NativeEvent` envelopes from a native event bus and falls back to mapped legacy events while migration continues; daemon smoke verifies lifecycle, `task.source.failed`, live BT metadata frames, and `task.bt.seeding.started` after deterministic WebSeed completion; native lifecycle, source-failure, progress, BT metadata, BT seeding, BT peer, and BT tracker helpers publish native task-id events; native projection drops unmapped legacy `Gid` events instead of synthesizing migration task ids; aria2 notification stream still remains | Migrate remaining protocol lifecycle assertions from JSON-RPC WebSocket to native events |
| Native CLI | raria commands and fields, no aria2 compatibility naming requirement | Partial | CLI has native `--api-port` for daemon control; many options remain aria2-shaped | Redesign remaining command groups and option schema around native API fields |
| Native configuration | `raria.toml` with serde validation | Partial | CLI `--conf-path` now loads strict native TOML, maps it into runtime config including `api.auth_token_file` and `[metalink]` mirror preferences, and `/api/v1/config` exposes runtime projection; old parser still exists internally | Add config docs and remove old parser from public/test surface |
| Native persistence | Versioned raria schema with migrations | Partial | redb exists; native metadata, task-row, and native segment tables are initialized and tested; range segment planning, checkpoint restore, checkpoint writes, and cleanup now enter through native task-id engine helpers; native task rows persist source health; old `Gid` segment rows are read-only migration fallback with focused coverage; native `/api/v1/session/save` and shutdown write native task rows from job-owned opaque task ids; daemon BT fastresume state is bound to a stable session-derived native state directory instead of the download directory | Remove runtime bridge/direct `Job` row reliance |
| User documentation | Modern raria docs, no compatibility claims | Partial | README now describes native `/api/v1`, event stream, and `raria.toml`; deeper docs still contain migration and compatibility wording | Continue rewriting docs around native raria model |
| Native identifier model | Opaque raria task identifiers, no aria2 GID semantics | Partial | Runtime jobs now carry opaque native `TaskId` values; registry lookup supports `TaskId`; daemon activation uses native task ids and returns a temporary executor bridge; range execution consumes a native descriptor for task options and output metadata; range completion, failure, daemon JSONL fields, and native event projection now use registered native task ids directly; native API creation, lookup, response projection, session save, and restart/restore preserve native ids | Move executor, cancellation, and remaining legacy event fields toward native `TaskId` and remove the bridge index |

## Core Runtime

| Capability | Modern target | raria status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Task model | Protocol-neutral task, file, source, segment, piece, peer, tracker, and event model | Partial | Native projections cover task summary, source, file, segment, piece, peer, tracker, event envelope, task rows, task id index, an engine native task facade, native id carried on runtime jobs, registry task-id lookup, native executor-facing helpers, native segment/rate-limiter helpers, native source/output metadata helpers, native lifecycle/source-failure helpers, native range segment planning helpers, a native range execution descriptor, and native terminal range operations; `Job` still drives persisted runtime state | Move engine ownership from migration `Job`/`Gid` to native task objects |
| Queue scheduling | Waiting, active, paused, stopped, seeding, completion, and bounded concurrency | Partial | Scheduler and status machine exist; scheduler queue storage is keyed by native `TaskId`; `/api/v1/tasks/{taskId}/queue` reads and updates absolute native queue position; daemon smoke verifies waiting-task queue reorder through the native route; native scheduler no longer counts seeding tasks against download concurrency and native BT seeding-start events wake the daemon queue loop; daemon native smoke verifies a waiting range task activates after a BT task enters seed-only lifecycle under `maxActiveTasks = 1`; legacy `Gid` queue methods remain as migration adapters; engine exposes native task activation handles; daemon activation uses native task ids | Add native priorities and remove legacy queue adapters when JSON-RPC is retired |
| Pause, resume, remove | Immediate and graceful lifecycle transitions across protocols | Partial | Native API exposes pause, resume, remove, and restart through the engine native task facade; daemon shutdown cancels active work through a native task operation | Move controls into native task service and `TaskId` ownership |
| Progress and global stats | Accurate per-task and global speed, completed bytes, active connections | Partial | `/api/v1/stats` exposes native task counts and speed fields; native progress helpers now publish native progress events; some runtime progress consumers still use the old event bus | Extend native stats and event stream coverage |
| Runtime options | Safe runtime mutation of limits, queue position, sources, and task settings | Partial | `/api/v1/tasks/{taskId}/queue` reads and updates absolute queue position; `/api/v1/tasks/{taskId}/sources` reads and replaces range task sources; `/api/v1/tasks/{taskId}/transfer` reads and updates native download/upload limits and segment count; daemon smoke verifies these mutations on queued tasks through `/api/v1` | Add BT source graph mutation and native priority policy |
| Structured logs | Durable operational logs with redaction and correlation IDs | Covered | `logging-contract.md`, structured logging helpers and tests; range daemon JSONL helpers now include native `task_id` alongside the temporary runtime `gid` | Keep expanding native task correlation during API migration |
| Hooks | Modern lifecycle hooks or event consumers | Partial | start, complete, error hooks exist | Decide native hook model after event schema |

## HTTP, HTTPS, FTP, FTPS, and SFTP

| Capability | Modern target | raria status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| HTTP/HTTPS downloads | reqwest-backed downloads with redirects, headers, auth, TLS, mTLS, cookies, proxy, and range support | Partial | `raria-http`, `single_download`, `http_config_smoke` | Re-anchor options to native config and API |
| FTP/FTPS downloads | suppaftp-backed FTP and explicit FTPS with resume and proxy support | Partial | `raria-ftp`, FTP and FTPS smoke tests | Re-anchor options and complete matrix cases |
| SFTP downloads | russh-backed SFTP with password, key auth, known_hosts, and proxy support | Partial | `raria-sftp`, SFTP smoke tests | Re-anchor options and complete matrix cases |
| Multi-source downloads | Multiple protocol sources for one task with failover and health scoring | Partial | URI registry and mirror failover exist; range daemon source selection now goes through a native task-id helper; native source projections include runtime health, failure counts, last errors, observed speed, and selection score; source selection now tests unobserved mirrors before preferring the healthiest unattempted source; session save/restore preserves source health through native task rows | Extend adaptive segmented behavior beyond per-source scoring |
| Adaptive segmented downloads | Dynamic segment planning, retry, cancellation, checkpointing, and limiter integration | Partial | `raria-range::SegmentExecutor`; range segment planning now uses a native engine helper that applies split constraints, selected-source health, and persisted checkpoint progress; degraded sources reduce planned range concurrency instead of blindly using the static split target | Add live segment rebalancing while a transfer is running |
| Resume and crash recovery | Robust resume across daemon restart and interrupted segments | Partial | native task-id segment planning, checkpoint restore/write callbacks, interrupted writes, and cleanup are routed through engine helpers; read-only old `Gid` segment fallback, session tests, native session save, native restart/restore smoke, daemon native segment resume smoke, focused fallback migration coverage, and daemon BT fastresume state directory binding coverage exist | Remove old `Gid` segment table after native schema migration coverage |
| Conditional requests | Conditional GET and resource-change handling | Partial | conditional GET and If-Range tests exist | Revalidate under native task model |
| Whole-file checksum | Multiple algorithms and terminal verification | Covered | `checksum.rs`, daemon verification path; daemon native Metalink smoke verifies a Metalink-provided SHA-256 mismatch fails the task and exposes the integrity error through native source health | Keep and expose natively |
| Piece checksum | Piece-level verification from Metalink and torrent metadata | Partial | Metalink piece checksum wiring and BT metadata; daemon native Metalink smoke verifies piece checksum mismatch fails the task and removes invalid output | Unify piece model across range and BT |
| Remote filename and metadata | Content-Disposition, remote timestamps, content type | Partial | Content-Disposition exists; remote-time is not fully covered | Add native metadata model |
| Cookie and netrc | Cookie load/save and netrc credential lookup | Partial | HTTP cookies and netrc support exist | Move into typed auth config |
| File allocation and disk strategy | none, preallocation, truncation, fallocate where supported | Partial | `file_alloc.rs`, executor allocation tests | Revalidate with native storage and resume |

## Metalink

| Capability | Modern target | raria status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Metalink v3 and v4 parsing | Parse modern Metalink inputs and normalize into native tasks | Partial | `raria-metalink` parser and normalizer now preserve v4 torrent `metaurl` entries as metadata sources instead of plain mirror URLs; `/api/v1/tasks` accepts native `metalink.bytesBase64` and `metalink.path` inputs and returns native task summaries; daemon smoke verifies native Metalink expansion completes and writes both files; the legacy `aria2.addMetalink` RPC method has been removed from the registered surface | Continue replacing lightweight graph metadata with native task graph modeling |
| Mirror priority and filters | Location, protocol preference, priority, and unique protocol handling | Partial | aria2 evidence: `MetalinkEntry` adjusts resource priority for preferred locations and protocols, then sorts resources; aria2 can also keep one source per protocol. raria normalizer now supports priority sorting, preferred locations, preferred protocol, and unique-protocol filtering through native options; `[metalink]` in `raria.toml` carries these preferences into dispatch and `/api/v1/config`; daemon native smoke verifies Metalink mirror failover after transfer failure and checksum mismatch | Broaden native daemon coverage for location/protocol filters |
| Checksums | Whole-file and piece checksums | Partial | Normalizer carries checksum fields; native API task creation preserves preferred whole-file checksum, expected size, and piece checksums from Metalink; daemon native smoke verifies whole-file checksum enforcement, piece checksum enforcement, invalid output cleanup, and native source-health error projection for Metalink tasks | Unify piece model across range and BT |
| Multi-file Metalink | Multiple files become native task graph | Partial | `/api/v1/tasks` creates a native task collection from Metalink bytes or path; daemon smoke verifies multi-file Metalink expansion and completion through the native API; JSON-RPC `aria2.addMetalink` and its lightweight relation tests have been removed | Replace lightweight relation fields with native graph model |
| Metaurl and torrent integration | Metalink torrent metaurl and hybrid downloads | Partial | aria2 evidence: Metalink v4 `metaurl mediatype="torrent"` is parsed as metadata and can group request groups; raria now parses torrent metaurls, normalizes them as metadata sources, dispatches torrent metaurls through the native BT metadata helper from `/api/v1/tasks`, carries Metalink mirrors as WebSeed URIs, fetches remote HTTP/HTTPS `.torrent` metadata into the BT runtime, and has daemon smoke proof that remote metadata plus WebSeed reaches native seeding lifecycle | Replace lightweight relation fields with native task graph modeling and broaden Metalink filter coverage |

## BitTorrent

| Capability | Modern target | raria status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Torrent file download | librqbit-backed torrent file ingestion and download | Partial | `raria-bt::BtSource::TorrentBytes/File`, smoke tests; native task creation treats `torrent:base64`, `torrent:` and `.torrent` path sources as BT backend tasks instead of range downloads; daemon native smoke creates `torrent:base64` tasks, announces to a tracker, exposes BT metadata through `/api/v1`, enters seeding after WebSeed-backed completion, accepts a local `.torrent` file path, fetches remote HTTP/HTTPS `.torrent` metadata before entering the WebSeed-backed seeding lifecycle, and no longer registers `aria2.addTorrent` | Add stop-timeout and seed-only daemon lifecycle coverage |
| Magnet metadata | Magnet ingestion and metadata resolution | Partial | librqbit magnet path exists; BT status sync publishes native `task.bt.metadata.resolved` events once metadata is available | Add daemon-level metadata lifecycle tests |
| DHT | DHT enablement and persistence | Partial | DHT persistence tests exist | Verify full behavior against librqbit docs and runtime tests |
| UDP tracker | UDP tracker support through the mature BitTorrent backend with native tracker URIs | Complete | `BtService` forwards native `bt.trackerUris` to librqbit `AddTorrentOptions.trackers`; `bt_service_downloads_real_torrent_through_udp_tracker` uses a local UDP tracker, discovers a local seed peer, completes a real torrent, and verifies the UDP announce URI remains visible in native status; `daemon_native_api_exposes_udp_bt_tracker_projection` verifies native task creation with a `udp://` tracker, real UDP announce use, native tracker projection, and task completion without aria2 fields | Keep under tracker-management regression coverage while adding timeout, interval, exclusion, and live replacement behavior |
| PEX | Peer exchange through the mature BitTorrent backend, with native policy carried from `raria.toml` | Partial | aria2 implements BEP-10 `ut_pex` with a default-enabled `enable-peer-exchange` option disabled for private torrents; librqbit's runtime extended handshake advertises `ut_pex` in `bt_service_advertises_ut_pex_in_extended_handshake`; `raria.toml` `bittorrent.enable_pex` now reaches `GlobalConfig` and `BtServiceConfig`; `bt_service_pex_disable_policy_is_not_enforced_by_backend_public_api` documents that current librqbit public APIs still advertise `ut_pex` when raria policy disables it | Keep policy field ready for upstream support or implement a small native peer-protocol gate only if needed after tracker and peer-state work |
| WebSeed | BEP-17/BEP-19 WebSeed support | Partial | `webseed.rs` and gap ledger tests exist; native task creation accepts `bt.webSeedUris`; daemon native smoke verifies a local WebSeed serves the torrent payload and the BT runtime reaches native seeding lifecycle through `/api/v1/events`; multi-file WebSeed daemon smoke verifies selected-file completion and cleanup behavior | Add explicit WebSeed progress/status projection |
| File selection | Select files before download and expose file progress | Partial | Native file projection uses BT file snapshots; `/api/v1/tasks/{taskId}/files` supports native selected file id updates; native task creation accepts `bt.selectedFileIds` and `bt.deleteUnselectedFilesOnCompletion`; daemon native smoke verifies initial selected file ids, live selection changes retained after BT runtime sync, and completion-time deletion of unselected files while preserving selected files; BT runtime polls native selected-file option changes and forwards them to librqbit `update_only_files`; legacy `select-file` RPC option still remains | Remove legacy RPC dependency and expand selected-file persistence coverage |
| Tracker management | Add, exclude, timeout, interval, and status projection | Partial | `NativeTrackerSnapshot` exists; `/api/v1/tasks/{taskId}/trackers` returns native tracker resources; daemon native smoke verifies live tracker projection and tracker announces for a `bt.trackerUris` task; daemon tracker smoke now creates BT tasks through `/api/v1/tasks`; `PATCH /api/v1/tasks/{taskId}/trackers` replaces tracker URIs and updates runtime tracker options; native task creation accepts `bt.trackerUris`; BT status sync publishes native tracker update events; native tracker resources now project `excluded`, `connectTimeoutSeconds`, `timeoutSeconds`, and `intervalSeconds`, and contract tests verify these policies persist through the native route; current librqbit 8.1.1 public API exposes add-time trackers but not an equivalent live tracker mutation API | Verify mature backend hooks for enforcing timeout, interval, exclusion, and live replacement; add a small raria-native live tracker layer only if needed |
| Peer projection | Expose peer list, speeds, and seeder status | Partial | `NativePeerSnapshot` exists; `/api/v1/tasks/{taskId}/peers` returns native peer resources; daemon native smoke verifies live peer projection without aria2 peer fields; BT status sync publishes native peer update events | Add richer peer state |
| Seeding controls | ratio, time, idle download stop timeout, seed-only lifecycle | Partial | seed ratio/time runtime logic exists; native task creation accepts `bt.seeding.targetRatio`, `bt.seeding.stopAfterMinutes`, and `bt.seeding.idleDownloadTimeoutSeconds`; `/api/v1/tasks/{taskId}/bt/seeding` reads and updates native fields without aria2 option names; daemon native smoke verifies `task.bt.seeding.started`, `lifecycle = seeding`, completed byte projection, and no aria2 event fields after WebSeed-backed completion; runtime unit coverage verifies incomplete BT downloads stop after a configured zero-speed idle window; scheduler coverage verifies seeding tasks do not consume download concurrency and seeding-start wakes queued task activation; daemon native smoke verifies seed-only queue activation under bounded concurrency | Add fastresume persistence binding coverage |
| Fastresume | Persist and restore BT progress | Partial | librqbit fastresume is enabled and tested; raria-bt accepts a native session persistence directory; daemon runtime binds the librqbit JSON fastresume folder to `session_file.bt-session`; daemon smoke verifies BT fastresume state is written to the session-derived native directory and not the old download-scoped default; docs.rs/upstream API review found stable `SessionPersistenceConfig::Json` folder wiring but no public API for embedding librqbit fastresume blobs directly inside redb | Add native metadata row for external BT state directory and restore validation across daemon restart |
| BT encryption | Modern transport policy only if supported by mature library | Excluded for MSE/ARC4 | Goal excludes MSE/ARC4 | Remove misleading config if unsupported |
| LPD | Local peer discovery | Excluded | Goal excludes LPD | Remove from matrix once no code references remain |

## Advanced Downloader Behavior

| Capability | Modern target | raria status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Rate limiting | Global and per-task download/upload limits | Partial | limiter and runtime mutation tests exist; `/api/v1/transfer` reads and updates global download limit, global upload limit policy, and max active task count; daemon smoke verifies global transfer mutation through `/api/v1`; `/api/v1/tasks/{taskId}/transfer` updates per-task download limiter, upload limit option, and segment count through native fields; range daemon obtains per-task limiter through a native task-id engine helper | Wire global upload limit into BT upload runtime enforcement |
| Retry policy | Typed transient/permanent classification and retry controls | Partial | executor retry and daemon classification exist | Move to typed error model |
| Mirror health | Server statistics and adaptive source selection | Partial | aria2 evidence: `ServerStat` records protocol/host status, speed, counter, and update time, while `AdaptiveURISelector` tests unobserved mirrors before selecting by speed; raria now records native source success/failure feedback, exposes source health through `/api/v1/tasks/{taskId}/sources`, prefers unobserved then highest-scoring unattempted mirrors, restores source health from native task rows, and feeds selected-source health into segment planning | Add live segment rebalancing while a transfer is running |
| File conflict policy | overwrite, auto-rename, partial reuse, selected-file cleanup | Partial | rename and overwrite options exist; native BT creation accepts `bt.deleteUnselectedFilesOnCompletion`; runtime and daemon smoke coverage verify unselected BT files are deleted after selected payload completion while selected files remain | Move remaining range conflict behavior to native file policy |
| Disk cache and mmap | Only if justified by measurable modern value | Gap | aria2 has disk cache and mmap options | Evaluate after core model migration |
| DNS and interface selection | Async DNS, bind interface, multiple interface, IPv6 controls | Gap | aria2 exposes these; raria does not model them fully | Verify library support and modern need |
| Process lifecycle | stop timer, stop with process, graceful signal handling | Partial | daemon handles Ctrl-C, SIGTERM, SIGUSR1; `POST /api/v1/daemon/shutdown` triggers the existing graceful shutdown token; daemon smoke verifies native shutdown stops the process and preserves BT fastresume, DHT shutdown persistence, and structured shutdown logs | Add native stop timer and stop-with-process policy |
| Security and redaction | Auth, token, TLS, secret redaction, path safety | Partial | Native bearer auth covers protected native API routes including stats and shutdown; daemon smoke verifies `raria.toml` token-file auth on `/api/v1`; RPC secret and old token-in-params path still remain | Remove JSON-RPC auth surface and add native secret redaction coverage |

## Architecture Decisions

| Decision | Status | Evidence | Next action |
| --- | --- | --- | --- |
| Preserve mature protocol libraries | Accepted | reqwest, suppaftp, russh, quick-xml, librqbit, redb, and axum verified against public docs | Keep verifying exact API behavior at each implementation slice |
| Use `/api/v1` resource API | Partial | Native health, config, task, stats, event, shutdown, session-save, and task-control contract tests pass; daemon smoke covers core lifecycle, native graceful shutdown, native explicit session save, and runtime mutation routes | Replace JSON-RPC as the daemon control surface |
| Use `/api/v1/events` WebSocket stream | Partial | Native WebSocket contract and daemon smoke tests pass for lifecycle, progress, and source-failure events | Migrate remaining protocol lifecycle assertions from JSON-RPC WebSocket to native stream |
| Use `raria.toml` strict schema | Partial | Native schema, runtime conversion including API bearer token loading, Metalink mirror preferences, CLI loading, daemon auth smoke, and `/api/v1/config` projection tests pass | Expand schema coverage and remove old config parser from public flow |
| Use versioned redb schemas | Partial | `NativeStoreMetadata` and `NativeTaskRow` DTOs exist; redb store initializes native metadata and task-row tables; save writes native rows including source health; restore reads native rows first | Add ID indexes, migration fixtures, and remove direct `Job` row reliance |

## Excluded Legacy Features

| Capability | Reason |
| --- | --- |
| XML-RPC | Legacy control surface, explicitly out of scope |
| libaria2 C API compatibility | Legacy embedding surface, explicitly out of scope |
| aria2 session/control-file compatibility | Legacy storage compatibility, explicitly out of scope |
| HTTP/1.1 pipelining | Historical HTTP behavior, explicitly out of scope |
| BitTorrent MSE/ARC4 | Legacy encryption, explicitly out of scope |
| LPD | Legacy local peer discovery, explicitly out of scope |
| aria2 config syntax compatibility | Replaced by `raria.toml` |
| Historical packaging and platform baggage | Non-core modernization target |
